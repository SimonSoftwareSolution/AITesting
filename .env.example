# Auth type: 'windows' = Microsoft/Azure AD SSO via Authentik, 'direct' = Authentik username+password
QUESTRA_AUTH_TYPE=windows

# Microsoft / Authentik credentials
QUESTRA_USERNAME=your-email@example.com
QUESTRA_PASSWORD=your-password
